close all
% set_path
global params U
U = [];
params = control_hyper_parameters();

params.t_target = 0.2;
params.kp_s = 300;
params.kp_t
params.kd_t
params.kd_s
sln = solve_eqns([0; 0; 0], [0;0;0], 20);
animate(sln);
analyze(sln);